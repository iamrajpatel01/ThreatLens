# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ScopeActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Scope]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Scope.prisma().query_raw(
            'SELECT * FROM Scope WHERE id = ?',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Scope
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Scope.prisma().query_first(
            'SELECT * FROM Scope WHERE domain = ?',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ScopeCreateInput,
        include: Optional[types.ScopeInclude] = None
    ) -> _PrismaModelT:
        """Create a new Scope record.

        Parameters
        ----------
        data
            Scope record data
        include
            Specifies which relations should be loaded on the returned Scope model

        Returns
        -------
        prisma.models.Scope
            The created Scope record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Scope record from just the required fields
        scope = await Scope.prisma().create(
            data={
                # data to create a Scope record
                'domain': 'ggciceaie',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ScopeCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Scope records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Scope record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Scope.prisma().create_many(
            data=[
                {
                    # data to create a Scope record
                    'domain': 'bbehjachib',
                },
                {
                    # data to create a Scope record
                    'domain': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ScopeWhereUniqueInput,
        include: Optional[types.ScopeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Scope record.

        Parameters
        ----------
        where
            Scope filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Scope model

        Returns
        -------
        prisma.models.Scope
            The deleted Scope record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scope = await Scope.prisma().delete(
            where={
                'id': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ScopeWhereUniqueInput,
        include: Optional[types.ScopeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Scope record.

        Parameters
        ----------
        where
            Scope filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Scope model

        Returns
        -------
        prisma.models.Scope
            The found Scope record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scope = await Scope.prisma().find_unique(
            where={
                'id': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ScopeWhereUniqueInput,
        include: Optional[types.ScopeInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Scope record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Scope filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Scope model

        Returns
        -------
        prisma.models.Scope
            The found Scope record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scope = await Scope.prisma().find_unique_or_raise(
            where={
                'id': 'eigcfgbif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScopeWhereInput] = None,
        cursor: Optional[types.ScopeWhereUniqueInput] = None,
        include: Optional[types.ScopeInclude] = None,
        order: Optional[Union[types.ScopeOrderByInput, List[types.ScopeOrderByInput]]] = None,
        distinct: Optional[List[types.ScopeScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Scope records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Scope records returned
        skip
            Ignore the first N results
        where
            Scope filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Scope model
        order
            Order the returned Scope records by any field
        distinct
            Filter Scope records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Scope]
            The list of all Scope records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Scope records
        scopes = await Scope.prisma().find_many(take=10)

        # find the first 5 Scope records ordered by the createdAt field
        scopes = await Scope.prisma().find_many(
            take=5,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScopeWhereInput] = None,
        cursor: Optional[types.ScopeWhereUniqueInput] = None,
        include: Optional[types.ScopeInclude] = None,
        order: Optional[Union[types.ScopeOrderByInput, List[types.ScopeOrderByInput]]] = None,
        distinct: Optional[List[types.ScopeScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Scope record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Scope filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Scope model
        order
            Order the returned Scope records by any field
        distinct
            Filter Scope records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Scope
            The first Scope record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Scope record ordered by the id field
        scope = await Scope.prisma().find_first(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScopeWhereInput] = None,
        cursor: Optional[types.ScopeWhereUniqueInput] = None,
        include: Optional[types.ScopeInclude] = None,
        order: Optional[Union[types.ScopeOrderByInput, List[types.ScopeOrderByInput]]] = None,
        distinct: Optional[List[types.ScopeScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Scope record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Scope filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Scope model
        order
            Order the returned Scope records by any field
        distinct
            Filter Scope records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Scope
            The first Scope record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Scope record ordered by the domain field
        scope = await Scope.prisma().find_first_or_raise(
            skip=1,
            order={
                'domain': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ScopeUpdateInput,
        where: types.ScopeWhereUniqueInput,
        include: Optional[types.ScopeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Scope record.

        Parameters
        ----------
        data
            Scope record data specifying what to update
        where
            Scope filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Scope model

        Returns
        -------
        prisma.models.Scope
            The updated Scope record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        scope = await Scope.prisma().update(
            where={
                'id': 'bagcfbhiig',
            },
            data={
                # data to update the Scope record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ScopeWhereUniqueInput,
        data: types.ScopeUpsertInput,
        include: Optional[types.ScopeInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Scope filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Scope model

        Returns
        -------
        prisma.models.Scope
            The created or updated Scope record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scope = await Scope.prisma().upsert(
            where={
                'id': 'cghideieh',
            },
            data={
                'create': {
                    'id': 'cghideieh',
                    'domain': 'cadfabfehe',
                },
                'update': {
                    'domain': 'cadfabfehe',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ScopeUpdateManyMutationInput,
        where: types.ScopeWhereInput,
    ) -> int:
        """Update multiple Scope records

        Parameters
        ----------
        data
            Scope data to update the selected Scope records to
        where
            Filter to select the Scope records to update

        Returns
        -------
        int
            The total number of Scope records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Scope records
        total = await Scope.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScopeWhereInput] = None,
        cursor: Optional[types.ScopeWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Scope records present in the database

        Parameters
        ----------
        select
            Select the Scope fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Scope filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScopeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Scope.prisma().count()

        # results: prisma.types.ScopeCountAggregateOutput
        results = await Scope.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ScopeCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScopeWhereInput] = None,
        cursor: Optional[types.ScopeWhereUniqueInput] = None,
    ) -> types.ScopeCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ScopeCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScopeWhereInput] = None,
        cursor: Optional[types.ScopeWhereUniqueInput] = None,
    ) -> Union[int, types.ScopeCountAggregateOutput]:
        """Count the number of Scope records present in the database

        Parameters
        ----------
        select
            Select the Scope fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Scope filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScopeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Scope.prisma().count()

        # results: prisma.types.ScopeCountAggregateOutput
        results = await Scope.prisma().count(
            select={
                '_all': True,
                'domain': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ScopeCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ScopeWhereInput] = None
    ) -> int:
        """Delete multiple Scope records.

        Parameters
        ----------
        where
            Optional Scope filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Scope records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Scope records
        total = await Scope.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ScopeScalarFieldKeys'],
        *,
        where: Optional['types.ScopeWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ScopeAvgAggregateInput'] = None,
        sum: Optional['types.ScopeSumAggregateInput'] = None,
        min: Optional['types.ScopeMinAggregateInput'] = None,
        max: Optional['types.ScopeMaxAggregateInput'] = None,
        having: Optional['types.ScopeScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ScopeCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ScopeScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ScopeScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ScopeGroupByOutput']:
        """Group Scope records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Scope fields to group records by
        where
            Scope filter to select records
        take
            Limit the maximum number of Scope records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ScopeGroupByOutput]
            A list of dictionaries representing the Scope record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Scope records by createdAt values
        # and count how many records are in each group
        results = await Scope.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AssetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Asset]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Asset.prisma().query_raw(
            'SELECT * FROM Asset WHERE id = ?',
            'biabhbdai',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Asset
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Asset.prisma().query_first(
            'SELECT * FROM Asset WHERE name = ?',
            'idghgaicb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AssetCreateInput,
        include: Optional[types.AssetInclude] = None
    ) -> _PrismaModelT:
        """Create a new Asset record.

        Parameters
        ----------
        data
            Asset record data
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The created Asset record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Asset record from just the required fields
        asset = await Asset.prisma().create(
            data={
                # data to create a Asset record
                'name': 'fjfddhigg',
                'type': 'hjaecfifb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AssetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Asset records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Asset record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Asset.prisma().create_many(
            data=[
                {
                    # data to create a Asset record
                    'name': 'cbbbjbfcii',
                    'type': 'bbejhfidcb',
                },
                {
                    # data to create a Asset record
                    'name': 'bgeecijdgg',
                    'type': 'bdiicjafbj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Asset record.

        Parameters
        ----------
        where
            Asset filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The deleted Asset record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().delete(
            where={
                'id': 'bgehebiafc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Asset record.

        Parameters
        ----------
        where
            Asset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The found Asset record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().find_unique(
            where={
                'id': 'bghffegacj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Asset record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Asset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The found Asset record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().find_unique_or_raise(
            where={
                'id': 'bhghchehcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Asset records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Asset records returned
        skip
            Ignore the first N results
        where
            Asset filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Asset]
            The list of all Asset records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Asset records
        assets = await Asset.prisma().find_many(take=10)

        # find the first 5 Asset records ordered by the type field
        assets = await Asset.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Asset record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Asset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Asset
            The first Asset record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Asset record ordered by the status field
        asset = await Asset.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Asset record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Asset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Asset
            The first Asset record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Asset record ordered by the sslStatus field
        asset = await Asset.prisma().find_first_or_raise(
            skip=1,
            order={
                'sslStatus': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AssetUpdateInput,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Asset record.

        Parameters
        ----------
        data
            Asset record data specifying what to update
        where
            Asset filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The updated Asset record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        asset = await Asset.prisma().update(
            where={
                'id': 'dcgchcbbf',
            },
            data={
                # data to update the Asset record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AssetWhereUniqueInput,
        data: types.AssetUpsertInput,
        include: Optional[types.AssetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Asset filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The created or updated Asset record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().upsert(
            where={
                'id': 'bdedcabahc',
            },
            data={
                'create': {
                    'id': 'bdedcabahc',
                    'name': 'bgeecijdgg',
                    'type': 'bdiicjafbj',
                },
                'update': {
                    'name': 'bgeecijdgg',
                    'type': 'bdiicjafbj',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AssetUpdateManyMutationInput,
        where: types.AssetWhereInput,
    ) -> int:
        """Update multiple Asset records

        Parameters
        ----------
        data
            Asset data to update the selected Asset records to
        where
            Filter to select the Asset records to update

        Returns
        -------
        int
            The total number of Asset records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Asset records
        total = await Asset.prisma().update_many(
            data={
                'sslExpiration': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Asset records present in the database

        Parameters
        ----------
        select
            Select the Asset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Asset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Asset.prisma().count()

        # results: prisma.types.AssetCountAggregateOutput
        results = await Asset.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AssetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> types.AssetCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AssetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> Union[int, types.AssetCountAggregateOutput]:
        """Count the number of Asset records present in the database

        Parameters
        ----------
        select
            Select the Asset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Asset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Asset.prisma().count()

        # results: prisma.types.AssetCountAggregateOutput
        results = await Asset.prisma().count(
            select={
                '_all': True,
                'lastSeen': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AssetCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AssetWhereInput] = None
    ) -> int:
        """Delete multiple Asset records.

        Parameters
        ----------
        where
            Optional Asset filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Asset records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Asset records
        total = await Asset.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AssetScalarFieldKeys'],
        *,
        where: Optional['types.AssetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AssetAvgAggregateInput'] = None,
        sum: Optional['types.AssetSumAggregateInput'] = None,
        min: Optional['types.AssetMinAggregateInput'] = None,
        max: Optional['types.AssetMaxAggregateInput'] = None,
        having: Optional['types.AssetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AssetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AssetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AssetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AssetGroupByOutput']:
        """Group Asset records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Asset fields to group records by
        where
            Asset filter to select records
        take
            Limit the maximum number of Asset records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AssetGroupByOutput]
            A list of dictionaries representing the Asset record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Asset records by scopeId values
        # and count how many records are in each group
        results = await Asset.prisma().group_by(
            ['scopeId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ServiceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Service]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Service.prisma().query_raw(
            'SELECT * FROM Service WHERE id = ?',
            'ghfhiafcb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Service
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Service.prisma().query_first(
            'SELECT * FROM Service WHERE port = ?',
            744964398,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ServiceCreateInput,
        include: Optional[types.ServiceInclude] = None
    ) -> _PrismaModelT:
        """Create a new Service record.

        Parameters
        ----------
        data
            Service record data
        include
            Specifies which relations should be loaded on the returned Service model

        Returns
        -------
        prisma.models.Service
            The created Service record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Service record from just the required fields
        service = await Service.prisma().create(
            data={
                # data to create a Service record
                'port': 1969681615,
                'protocol': 'bbbgbhfjge',
                'techStack': 'igbehcbab',
                'endpoints': 'bdadaadhag',
                'assetId': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ServiceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Service records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Service record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Service.prisma().create_many(
            data=[
                {
                    # data to create a Service record
                    'port': 2000430152,
                    'protocol': 'bigibebcib',
                    'techStack': 'bigaiehgcc',
                    'endpoints': 'beeifcbebf',
                    'assetId': 'bgcigfahea',
                },
                {
                    # data to create a Service record
                    'port': 1249606685,
                    'protocol': 'idfjadbcc',
                    'techStack': 'hgdhbjhhj',
                    'endpoints': 'ecjjjfbae',
                    'assetId': 'bhhfibbigf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ServiceWhereUniqueInput,
        include: Optional[types.ServiceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Service record.

        Parameters
        ----------
        where
            Service filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Service model

        Returns
        -------
        prisma.models.Service
            The deleted Service record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        service = await Service.prisma().delete(
            where={
                'id': 'ijdbeffgg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ServiceWhereUniqueInput,
        include: Optional[types.ServiceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Service record.

        Parameters
        ----------
        where
            Service filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Service model

        Returns
        -------
        prisma.models.Service
            The found Service record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        service = await Service.prisma().find_unique(
            where={
                'id': 'jjfeafhfj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ServiceWhereUniqueInput,
        include: Optional[types.ServiceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Service record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Service filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Service model

        Returns
        -------
        prisma.models.Service
            The found Service record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        service = await Service.prisma().find_unique_or_raise(
            where={
                'id': 'cbachdgfce',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ServiceWhereInput] = None,
        cursor: Optional[types.ServiceWhereUniqueInput] = None,
        include: Optional[types.ServiceInclude] = None,
        order: Optional[Union[types.ServiceOrderByInput, List[types.ServiceOrderByInput]]] = None,
        distinct: Optional[List[types.ServiceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Service records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Service records returned
        skip
            Ignore the first N results
        where
            Service filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Service model
        order
            Order the returned Service records by any field
        distinct
            Filter Service records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Service]
            The list of all Service records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Service records
        services = await Service.prisma().find_many(take=10)

        # find the first 5 Service records ordered by the protocol field
        services = await Service.prisma().find_many(
            take=5,
            order={
                'protocol': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ServiceWhereInput] = None,
        cursor: Optional[types.ServiceWhereUniqueInput] = None,
        include: Optional[types.ServiceInclude] = None,
        order: Optional[Union[types.ServiceOrderByInput, List[types.ServiceOrderByInput]]] = None,
        distinct: Optional[List[types.ServiceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Service record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Service filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Service model
        order
            Order the returned Service records by any field
        distinct
            Filter Service records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Service
            The first Service record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Service record ordered by the techStack field
        service = await Service.prisma().find_first(
            skip=1,
            order={
                'techStack': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ServiceWhereInput] = None,
        cursor: Optional[types.ServiceWhereUniqueInput] = None,
        include: Optional[types.ServiceInclude] = None,
        order: Optional[Union[types.ServiceOrderByInput, List[types.ServiceOrderByInput]]] = None,
        distinct: Optional[List[types.ServiceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Service record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Service filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Service model
        order
            Order the returned Service records by any field
        distinct
            Filter Service records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Service
            The first Service record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Service record ordered by the endpoints field
        service = await Service.prisma().find_first_or_raise(
            skip=1,
            order={
                'endpoints': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ServiceUpdateInput,
        where: types.ServiceWhereUniqueInput,
        include: Optional[types.ServiceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Service record.

        Parameters
        ----------
        data
            Service record data specifying what to update
        where
            Service filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Service model

        Returns
        -------
        prisma.models.Service
            The updated Service record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        service = await Service.prisma().update(
            where={
                'id': 'chbfcacbd',
            },
            data={
                # data to update the Service record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ServiceWhereUniqueInput,
        data: types.ServiceUpsertInput,
        include: Optional[types.ServiceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Service filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Service model

        Returns
        -------
        prisma.models.Service
            The created or updated Service record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        service = await Service.prisma().upsert(
            where={
                'id': 'efggddide',
            },
            data={
                'create': {
                    'id': 'efggddide',
                    'port': 1249606685,
                    'protocol': 'idfjadbcc',
                    'techStack': 'hgdhbjhhj',
                    'endpoints': 'ecjjjfbae',
                    'assetId': 'bhhfibbigf',
                },
                'update': {
                    'port': 1249606685,
                    'protocol': 'idfjadbcc',
                    'techStack': 'hgdhbjhhj',
                    'endpoints': 'ecjjjfbae',
                    'assetId': 'bhhfibbigf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ServiceUpdateManyMutationInput,
        where: types.ServiceWhereInput,
    ) -> int:
        """Update multiple Service records

        Parameters
        ----------
        data
            Service data to update the selected Service records to
        where
            Filter to select the Service records to update

        Returns
        -------
        int
            The total number of Service records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Service records
        total = await Service.prisma().update_many(
            data={
                'assetId': 'caficfigfb'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ServiceWhereInput] = None,
        cursor: Optional[types.ServiceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Service records present in the database

        Parameters
        ----------
        select
            Select the Service fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Service filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ServiceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Service.prisma().count()

        # results: prisma.types.ServiceCountAggregateOutput
        results = await Service.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ServiceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ServiceWhereInput] = None,
        cursor: Optional[types.ServiceWhereUniqueInput] = None,
    ) -> types.ServiceCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ServiceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ServiceWhereInput] = None,
        cursor: Optional[types.ServiceWhereUniqueInput] = None,
    ) -> Union[int, types.ServiceCountAggregateOutput]:
        """Count the number of Service records present in the database

        Parameters
        ----------
        select
            Select the Service fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Service filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ServiceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Service.prisma().count()

        # results: prisma.types.ServiceCountAggregateOutput
        results = await Service.prisma().count(
            select={
                '_all': True,
                'port': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ServiceCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ServiceWhereInput] = None
    ) -> int:
        """Delete multiple Service records.

        Parameters
        ----------
        where
            Optional Service filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Service records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Service records
        total = await Service.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ServiceScalarFieldKeys'],
        *,
        where: Optional['types.ServiceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ServiceAvgAggregateInput'] = None,
        sum: Optional['types.ServiceSumAggregateInput'] = None,
        min: Optional['types.ServiceMinAggregateInput'] = None,
        max: Optional['types.ServiceMaxAggregateInput'] = None,
        having: Optional['types.ServiceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ServiceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ServiceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ServiceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ServiceGroupByOutput']:
        """Group Service records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Service fields to group records by
        where
            Service filter to select records
        take
            Limit the maximum number of Service records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ServiceGroupByOutput]
            A list of dictionaries representing the Service record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Service records by protocol values
        # and count how many records are in each group
        results = await Service.prisma().group_by(
            ['protocol'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IdentityActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Identity]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Identity.prisma().query_raw(
            'SELECT * FROM Identity WHERE id = ?',
            'bfidgijfjc',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Identity
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Identity.prisma().query_first(
            'SELECT * FROM Identity WHERE email = ?',
            'ihieecagf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.IdentityCreateInput,
        include: Optional[types.IdentityInclude] = None
    ) -> _PrismaModelT:
        """Create a new Identity record.

        Parameters
        ----------
        data
            Identity record data
        include
            Specifies which relations should be loaded on the returned Identity model

        Returns
        -------
        prisma.models.Identity
            The created Identity record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Identity record from just the required fields
        identity = await Identity.prisma().create(
            data={
                # data to create a Identity record
                'source': 'bghfciaafe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.IdentityCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Identity records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Identity record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Identity.prisma().create_many(
            data=[
                {
                    # data to create a Identity record
                    'source': 'bgchfhgceh',
                },
                {
                    # data to create a Identity record
                    'source': 'cafeiaccbc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.IdentityWhereUniqueInput,
        include: Optional[types.IdentityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Identity record.

        Parameters
        ----------
        where
            Identity filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Identity model

        Returns
        -------
        prisma.models.Identity
            The deleted Identity record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        identity = await Identity.prisma().delete(
            where={
                'id': 'gaddfhfh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.IdentityWhereUniqueInput,
        include: Optional[types.IdentityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Identity record.

        Parameters
        ----------
        where
            Identity filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Identity model

        Returns
        -------
        prisma.models.Identity
            The found Identity record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        identity = await Identity.prisma().find_unique(
            where={
                'id': 'gieegcbeg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.IdentityWhereUniqueInput,
        include: Optional[types.IdentityInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Identity record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Identity filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Identity model

        Returns
        -------
        prisma.models.Identity
            The found Identity record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        identity = await Identity.prisma().find_unique_or_raise(
            where={
                'id': 'bgcffadich',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdentityWhereInput] = None,
        cursor: Optional[types.IdentityWhereUniqueInput] = None,
        include: Optional[types.IdentityInclude] = None,
        order: Optional[Union[types.IdentityOrderByInput, List[types.IdentityOrderByInput]]] = None,
        distinct: Optional[List[types.IdentityScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Identity records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Identity records returned
        skip
            Ignore the first N results
        where
            Identity filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Identity model
        order
            Order the returned Identity records by any field
        distinct
            Filter Identity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Identity]
            The list of all Identity records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Identity records
        identitys = await Identity.prisma().find_many(take=10)

        # find the first 5 Identity records ordered by the role field
        identitys = await Identity.prisma().find_many(
            take=5,
            order={
                'role': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IdentityWhereInput] = None,
        cursor: Optional[types.IdentityWhereUniqueInput] = None,
        include: Optional[types.IdentityInclude] = None,
        order: Optional[Union[types.IdentityOrderByInput, List[types.IdentityOrderByInput]]] = None,
        distinct: Optional[List[types.IdentityScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Identity record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Identity filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Identity model
        order
            Order the returned Identity records by any field
        distinct
            Filter Identity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Identity
            The first Identity record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Identity record ordered by the source field
        identity = await Identity.prisma().find_first(
            skip=1,
            order={
                'source': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IdentityWhereInput] = None,
        cursor: Optional[types.IdentityWhereUniqueInput] = None,
        include: Optional[types.IdentityInclude] = None,
        order: Optional[Union[types.IdentityOrderByInput, List[types.IdentityOrderByInput]]] = None,
        distinct: Optional[List[types.IdentityScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Identity record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Identity filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Identity model
        order
            Order the returned Identity records by any field
        distinct
            Filter Identity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Identity
            The first Identity record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Identity record ordered by the leakedAt field
        identity = await Identity.prisma().find_first_or_raise(
            skip=1,
            order={
                'leakedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.IdentityUpdateInput,
        where: types.IdentityWhereUniqueInput,
        include: Optional[types.IdentityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Identity record.

        Parameters
        ----------
        data
            Identity record data specifying what to update
        where
            Identity filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Identity model

        Returns
        -------
        prisma.models.Identity
            The updated Identity record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        identity = await Identity.prisma().update(
            where={
                'id': 'fcbichhci',
            },
            data={
                # data to update the Identity record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.IdentityWhereUniqueInput,
        data: types.IdentityUpsertInput,
        include: Optional[types.IdentityInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Identity filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Identity model

        Returns
        -------
        prisma.models.Identity
            The created or updated Identity record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        identity = await Identity.prisma().upsert(
            where={
                'id': 'bcggadccgf',
            },
            data={
                'create': {
                    'id': 'bcggadccgf',
                    'source': 'cafeiaccbc',
                },
                'update': {
                    'source': 'cafeiaccbc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.IdentityUpdateManyMutationInput,
        where: types.IdentityWhereInput,
    ) -> int:
        """Update multiple Identity records

        Parameters
        ----------
        data
            Identity data to update the selected Identity records to
        where
            Filter to select the Identity records to update

        Returns
        -------
        int
            The total number of Identity records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Identity records
        total = await Identity.prisma().update_many(
            data={
                'serviceId': 'jdcfdcgc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdentityWhereInput] = None,
        cursor: Optional[types.IdentityWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Identity records present in the database

        Parameters
        ----------
        select
            Select the Identity fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Identity filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IdentityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Identity.prisma().count()

        # results: prisma.types.IdentityCountAggregateOutput
        results = await Identity.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.IdentityCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdentityWhereInput] = None,
        cursor: Optional[types.IdentityWhereUniqueInput] = None,
    ) -> types.IdentityCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.IdentityCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdentityWhereInput] = None,
        cursor: Optional[types.IdentityWhereUniqueInput] = None,
    ) -> Union[int, types.IdentityCountAggregateOutput]:
        """Count the number of Identity records present in the database

        Parameters
        ----------
        select
            Select the Identity fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Identity filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IdentityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Identity.prisma().count()

        # results: prisma.types.IdentityCountAggregateOutput
        results = await Identity.prisma().count(
            select={
                '_all': True,
                'email': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IdentityCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.IdentityWhereInput] = None
    ) -> int:
        """Delete multiple Identity records.

        Parameters
        ----------
        where
            Optional Identity filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Identity records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Identity records
        total = await Identity.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.IdentityScalarFieldKeys'],
        *,
        where: Optional['types.IdentityWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IdentityAvgAggregateInput'] = None,
        sum: Optional['types.IdentitySumAggregateInput'] = None,
        min: Optional['types.IdentityMinAggregateInput'] = None,
        max: Optional['types.IdentityMaxAggregateInput'] = None,
        having: Optional['types.IdentityScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IdentityCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IdentityScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IdentityScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IdentityGroupByOutput']:
        """Group Identity records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Identity fields to group records by
        where
            Identity filter to select records
        take
            Limit the maximum number of Identity records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IdentityGroupByOutput]
            A list of dictionaries representing the Identity record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Identity records by role values
        # and count how many records are in each group
        results = await Identity.prisma().group_by(
            ['role'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class VulnerabilityActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Vulnerability]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Vulnerability.prisma().query_raw(
            'SELECT * FROM Vulnerability WHERE id = ?',
            'cafdaehjid',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Vulnerability
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Vulnerability.prisma().query_first(
            'SELECT * FROM Vulnerability WHERE title = ?',
            'gifdddbia',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.VulnerabilityCreateInput,
        include: Optional[types.VulnerabilityInclude] = None
    ) -> _PrismaModelT:
        """Create a new Vulnerability record.

        Parameters
        ----------
        data
            Vulnerability record data
        include
            Specifies which relations should be loaded on the returned Vulnerability model

        Returns
        -------
        prisma.models.Vulnerability
            The created Vulnerability record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Vulnerability record from just the required fields
        vulnerability = await Vulnerability.prisma().create(
            data={
                # data to create a Vulnerability record
                'title': 'bchehecef',
                'type': 'jeijcbhfe',
                'severity': 'bjgejjabff',
                'description': 'bcciijbibg',
                'context': 'cffcachfd',
                'attackPathSequence': 'bccdfhdigc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.VulnerabilityCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Vulnerability records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Vulnerability record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Vulnerability.prisma().create_many(
            data=[
                {
                    # data to create a Vulnerability record
                    'title': 'febcgjbfj',
                    'type': 'bageiegghg',
                    'severity': 'faidicegb',
                    'description': 'bacecgfhbe',
                    'context': 'ihcahiead',
                    'attackPathSequence': 'biheheiajg',
                },
                {
                    # data to create a Vulnerability record
                    'title': 'jbgijghgb',
                    'type': 'hgjcghfbi',
                    'severity': 'icadbcehj',
                    'description': 'jchciaee',
                    'context': 'deeificjd',
                    'attackPathSequence': 'bbcbhebbda',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.VulnerabilityWhereUniqueInput,
        include: Optional[types.VulnerabilityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Vulnerability record.

        Parameters
        ----------
        where
            Vulnerability filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Vulnerability model

        Returns
        -------
        prisma.models.Vulnerability
            The deleted Vulnerability record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vulnerability = await Vulnerability.prisma().delete(
            where={
                'id': 'bejfijgcfb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.VulnerabilityWhereUniqueInput,
        include: Optional[types.VulnerabilityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Vulnerability record.

        Parameters
        ----------
        where
            Vulnerability filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Vulnerability model

        Returns
        -------
        prisma.models.Vulnerability
            The found Vulnerability record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vulnerability = await Vulnerability.prisma().find_unique(
            where={
                'id': 'caifcbgii',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.VulnerabilityWhereUniqueInput,
        include: Optional[types.VulnerabilityInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Vulnerability record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Vulnerability filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Vulnerability model

        Returns
        -------
        prisma.models.Vulnerability
            The found Vulnerability record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vulnerability = await Vulnerability.prisma().find_unique_or_raise(
            where={
                'id': 'igaibbfgj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VulnerabilityWhereInput] = None,
        cursor: Optional[types.VulnerabilityWhereUniqueInput] = None,
        include: Optional[types.VulnerabilityInclude] = None,
        order: Optional[Union[types.VulnerabilityOrderByInput, List[types.VulnerabilityOrderByInput]]] = None,
        distinct: Optional[List[types.VulnerabilityScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Vulnerability records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Vulnerability records returned
        skip
            Ignore the first N results
        where
            Vulnerability filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Vulnerability model
        order
            Order the returned Vulnerability records by any field
        distinct
            Filter Vulnerability records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Vulnerability]
            The list of all Vulnerability records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Vulnerability records
        vulnerabilitys = await Vulnerability.prisma().find_many(take=10)

        # find the first 5 Vulnerability records ordered by the type field
        vulnerabilitys = await Vulnerability.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VulnerabilityWhereInput] = None,
        cursor: Optional[types.VulnerabilityWhereUniqueInput] = None,
        include: Optional[types.VulnerabilityInclude] = None,
        order: Optional[Union[types.VulnerabilityOrderByInput, List[types.VulnerabilityOrderByInput]]] = None,
        distinct: Optional[List[types.VulnerabilityScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Vulnerability record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Vulnerability filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Vulnerability model
        order
            Order the returned Vulnerability records by any field
        distinct
            Filter Vulnerability records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Vulnerability
            The first Vulnerability record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Vulnerability record ordered by the severity field
        vulnerability = await Vulnerability.prisma().find_first(
            skip=1,
            order={
                'severity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VulnerabilityWhereInput] = None,
        cursor: Optional[types.VulnerabilityWhereUniqueInput] = None,
        include: Optional[types.VulnerabilityInclude] = None,
        order: Optional[Union[types.VulnerabilityOrderByInput, List[types.VulnerabilityOrderByInput]]] = None,
        distinct: Optional[List[types.VulnerabilityScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Vulnerability record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Vulnerability filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Vulnerability model
        order
            Order the returned Vulnerability records by any field
        distinct
            Filter Vulnerability records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Vulnerability
            The first Vulnerability record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Vulnerability record ordered by the description field
        vulnerability = await Vulnerability.prisma().find_first_or_raise(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.VulnerabilityUpdateInput,
        where: types.VulnerabilityWhereUniqueInput,
        include: Optional[types.VulnerabilityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Vulnerability record.

        Parameters
        ----------
        data
            Vulnerability record data specifying what to update
        where
            Vulnerability filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Vulnerability model

        Returns
        -------
        prisma.models.Vulnerability
            The updated Vulnerability record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        vulnerability = await Vulnerability.prisma().update(
            where={
                'id': 'bggajdcbbi',
            },
            data={
                # data to update the Vulnerability record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.VulnerabilityWhereUniqueInput,
        data: types.VulnerabilityUpsertInput,
        include: Optional[types.VulnerabilityInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Vulnerability filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Vulnerability model

        Returns
        -------
        prisma.models.Vulnerability
            The created or updated Vulnerability record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vulnerability = await Vulnerability.prisma().upsert(
            where={
                'id': 'fcfhgbjed',
            },
            data={
                'create': {
                    'id': 'fcfhgbjed',
                    'title': 'jbgijghgb',
                    'type': 'hgjcghfbi',
                    'severity': 'icadbcehj',
                    'description': 'jchciaee',
                    'context': 'deeificjd',
                    'attackPathSequence': 'bbcbhebbda',
                },
                'update': {
                    'title': 'jbgijghgb',
                    'type': 'hgjcghfbi',
                    'severity': 'icadbcehj',
                    'description': 'jchciaee',
                    'context': 'deeificjd',
                    'attackPathSequence': 'bbcbhebbda',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.VulnerabilityUpdateManyMutationInput,
        where: types.VulnerabilityWhereInput,
    ) -> int:
        """Update multiple Vulnerability records

        Parameters
        ----------
        data
            Vulnerability data to update the selected Vulnerability records to
        where
            Filter to select the Vulnerability records to update

        Returns
        -------
        int
            The total number of Vulnerability records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Vulnerability records
        total = await Vulnerability.prisma().update_many(
            data={
                'context': 'hdgcajhjg'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VulnerabilityWhereInput] = None,
        cursor: Optional[types.VulnerabilityWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Vulnerability records present in the database

        Parameters
        ----------
        select
            Select the Vulnerability fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Vulnerability filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VulnerabilityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Vulnerability.prisma().count()

        # results: prisma.types.VulnerabilityCountAggregateOutput
        results = await Vulnerability.prisma().count(
            select={
                '_all': True,
                'attackPathSequence': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.VulnerabilityCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VulnerabilityWhereInput] = None,
        cursor: Optional[types.VulnerabilityWhereUniqueInput] = None,
    ) -> types.VulnerabilityCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.VulnerabilityCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VulnerabilityWhereInput] = None,
        cursor: Optional[types.VulnerabilityWhereUniqueInput] = None,
    ) -> Union[int, types.VulnerabilityCountAggregateOutput]:
        """Count the number of Vulnerability records present in the database

        Parameters
        ----------
        select
            Select the Vulnerability fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Vulnerability filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VulnerabilityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Vulnerability.prisma().count()

        # results: prisma.types.VulnerabilityCountAggregateOutput
        results = await Vulnerability.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.VulnerabilityCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.VulnerabilityWhereInput] = None
    ) -> int:
        """Delete multiple Vulnerability records.

        Parameters
        ----------
        where
            Optional Vulnerability filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Vulnerability records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Vulnerability records
        total = await Vulnerability.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.VulnerabilityScalarFieldKeys'],
        *,
        where: Optional['types.VulnerabilityWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.VulnerabilityAvgAggregateInput'] = None,
        sum: Optional['types.VulnerabilitySumAggregateInput'] = None,
        min: Optional['types.VulnerabilityMinAggregateInput'] = None,
        max: Optional['types.VulnerabilityMaxAggregateInput'] = None,
        having: Optional['types.VulnerabilityScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.VulnerabilityCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.VulnerabilityScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.VulnerabilityScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.VulnerabilityGroupByOutput']:
        """Group Vulnerability records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Vulnerability fields to group records by
        where
            Vulnerability filter to select records
        take
            Limit the maximum number of Vulnerability records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.VulnerabilityGroupByOutput]
            A list of dictionaries representing the Vulnerability record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Vulnerability records by id values
        # and count how many records are in each group
        results = await Vulnerability.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class EvidenceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Evidence]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Evidence.prisma().query_raw(
            'SELECT * FROM Evidence WHERE id = ?',
            'ejdjahicb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Evidence
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Evidence.prisma().query_first(
            'SELECT * FROM Evidence WHERE requestA = ?',
            'gdjgigfgc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.EvidenceCreateInput,
        include: Optional[types.EvidenceInclude] = None
    ) -> _PrismaModelT:
        """Create a new Evidence record.

        Parameters
        ----------
        data
            Evidence record data
        include
            Specifies which relations should be loaded on the returned Evidence model

        Returns
        -------
        prisma.models.Evidence
            The created Evidence record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Evidence record from just the required fields
        evidence = await Evidence.prisma().create(
            data={
                # data to create a Evidence record
                'requestA': 'gfeaahdeh',
                'requestB': 'bjafcgbffc',
                'responseA': 'hihegjif',
                'responseB': 'bdjidcidac',
                'vulnerabilityId': 'ifgaaagff',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.EvidenceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Evidence records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Evidence record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Evidence.prisma().create_many(
            data=[
                {
                    # data to create a Evidence record
                    'requestA': 'befcddgjce',
                    'requestB': 'bfhdbjjgfd',
                    'responseA': 'cabdjadaji',
                    'responseB': 'faajgfadf',
                    'vulnerabilityId': 'biaagcedjc',
                },
                {
                    # data to create a Evidence record
                    'requestA': 'cahhaghecf',
                    'requestB': 'bghcbbcidi',
                    'responseA': 'jcgghhgdj',
                    'responseB': 'beehgcebbg',
                    'vulnerabilityId': 'bhdiaidiaf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.EvidenceWhereUniqueInput,
        include: Optional[types.EvidenceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Evidence record.

        Parameters
        ----------
        where
            Evidence filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Evidence model

        Returns
        -------
        prisma.models.Evidence
            The deleted Evidence record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        evidence = await Evidence.prisma().delete(
            where={
                'id': 'deajegcfi',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.EvidenceWhereUniqueInput,
        include: Optional[types.EvidenceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Evidence record.

        Parameters
        ----------
        where
            Evidence filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Evidence model

        Returns
        -------
        prisma.models.Evidence
            The found Evidence record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        evidence = await Evidence.prisma().find_unique(
            where={
                'id': 'gabahhhjf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.EvidenceWhereUniqueInput,
        include: Optional[types.EvidenceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Evidence record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Evidence filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Evidence model

        Returns
        -------
        prisma.models.Evidence
            The found Evidence record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        evidence = await Evidence.prisma().find_unique_or_raise(
            where={
                'id': 'cjagadcjg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EvidenceWhereInput] = None,
        cursor: Optional[types.EvidenceWhereUniqueInput] = None,
        include: Optional[types.EvidenceInclude] = None,
        order: Optional[Union[types.EvidenceOrderByInput, List[types.EvidenceOrderByInput]]] = None,
        distinct: Optional[List[types.EvidenceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Evidence records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Evidence records returned
        skip
            Ignore the first N results
        where
            Evidence filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Evidence model
        order
            Order the returned Evidence records by any field
        distinct
            Filter Evidence records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Evidence]
            The list of all Evidence records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Evidence records
        evidences = await Evidence.prisma().find_many(take=10)

        # find the first 5 Evidence records ordered by the requestB field
        evidences = await Evidence.prisma().find_many(
            take=5,
            order={
                'requestB': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EvidenceWhereInput] = None,
        cursor: Optional[types.EvidenceWhereUniqueInput] = None,
        include: Optional[types.EvidenceInclude] = None,
        order: Optional[Union[types.EvidenceOrderByInput, List[types.EvidenceOrderByInput]]] = None,
        distinct: Optional[List[types.EvidenceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Evidence record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Evidence filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Evidence model
        order
            Order the returned Evidence records by any field
        distinct
            Filter Evidence records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Evidence
            The first Evidence record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Evidence record ordered by the responseA field
        evidence = await Evidence.prisma().find_first(
            skip=1,
            order={
                'responseA': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EvidenceWhereInput] = None,
        cursor: Optional[types.EvidenceWhereUniqueInput] = None,
        include: Optional[types.EvidenceInclude] = None,
        order: Optional[Union[types.EvidenceOrderByInput, List[types.EvidenceOrderByInput]]] = None,
        distinct: Optional[List[types.EvidenceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Evidence record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Evidence filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Evidence model
        order
            Order the returned Evidence records by any field
        distinct
            Filter Evidence records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Evidence
            The first Evidence record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Evidence record ordered by the responseB field
        evidence = await Evidence.prisma().find_first_or_raise(
            skip=1,
            order={
                'responseB': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.EvidenceUpdateInput,
        where: types.EvidenceWhereUniqueInput,
        include: Optional[types.EvidenceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Evidence record.

        Parameters
        ----------
        data
            Evidence record data specifying what to update
        where
            Evidence filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Evidence model

        Returns
        -------
        prisma.models.Evidence
            The updated Evidence record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        evidence = await Evidence.prisma().update(
            where={
                'id': 'bifficggej',
            },
            data={
                # data to update the Evidence record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.EvidenceWhereUniqueInput,
        data: types.EvidenceUpsertInput,
        include: Optional[types.EvidenceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Evidence filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Evidence model

        Returns
        -------
        prisma.models.Evidence
            The created or updated Evidence record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        evidence = await Evidence.prisma().upsert(
            where={
                'id': 'bgbbaajbic',
            },
            data={
                'create': {
                    'id': 'bgbbaajbic',
                    'requestA': 'cahhaghecf',
                    'requestB': 'bghcbbcidi',
                    'responseA': 'jcgghhgdj',
                    'responseB': 'beehgcebbg',
                    'vulnerabilityId': 'bhdiaidiaf',
                },
                'update': {
                    'requestA': 'cahhaghecf',
                    'requestB': 'bghcbbcidi',
                    'responseA': 'jcgghhgdj',
                    'responseB': 'beehgcebbg',
                    'vulnerabilityId': 'bhdiaidiaf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.EvidenceUpdateManyMutationInput,
        where: types.EvidenceWhereInput,
    ) -> int:
        """Update multiple Evidence records

        Parameters
        ----------
        data
            Evidence data to update the selected Evidence records to
        where
            Filter to select the Evidence records to update

        Returns
        -------
        int
            The total number of Evidence records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Evidence records
        total = await Evidence.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EvidenceWhereInput] = None,
        cursor: Optional[types.EvidenceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Evidence records present in the database

        Parameters
        ----------
        select
            Select the Evidence fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Evidence filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EvidenceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Evidence.prisma().count()

        # results: prisma.types.EvidenceCountAggregateOutput
        results = await Evidence.prisma().count(
            select={
                '_all': True,
                'vulnerabilityId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.EvidenceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EvidenceWhereInput] = None,
        cursor: Optional[types.EvidenceWhereUniqueInput] = None,
    ) -> types.EvidenceCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.EvidenceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EvidenceWhereInput] = None,
        cursor: Optional[types.EvidenceWhereUniqueInput] = None,
    ) -> Union[int, types.EvidenceCountAggregateOutput]:
        """Count the number of Evidence records present in the database

        Parameters
        ----------
        select
            Select the Evidence fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Evidence filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EvidenceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Evidence.prisma().count()

        # results: prisma.types.EvidenceCountAggregateOutput
        results = await Evidence.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.EvidenceCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.EvidenceWhereInput] = None
    ) -> int:
        """Delete multiple Evidence records.

        Parameters
        ----------
        where
            Optional Evidence filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Evidence records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Evidence records
        total = await Evidence.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.EvidenceScalarFieldKeys'],
        *,
        where: Optional['types.EvidenceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.EvidenceAvgAggregateInput'] = None,
        sum: Optional['types.EvidenceSumAggregateInput'] = None,
        min: Optional['types.EvidenceMinAggregateInput'] = None,
        max: Optional['types.EvidenceMaxAggregateInput'] = None,
        having: Optional['types.EvidenceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.EvidenceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.EvidenceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.EvidenceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.EvidenceGroupByOutput']:
        """Group Evidence records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Evidence fields to group records by
        where
            Evidence filter to select records
        take
            Limit the maximum number of Evidence records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.EvidenceGroupByOutput]
            A list of dictionaries representing the Evidence record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Evidence records by requestA values
        # and count how many records are in each group
        results = await Evidence.prisma().group_by(
            ['requestA'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class RemediationActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Remediation]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Remediation.prisma().query_raw(
            'SELECT * FROM Remediation WHERE id = ?',
            'eegghdhjb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Remediation
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Remediation.prisma().query_first(
            'SELECT * FROM Remediation WHERE title = ?',
            'daafgidjg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.RemediationCreateInput,
        include: Optional[types.RemediationInclude] = None
    ) -> _PrismaModelT:
        """Create a new Remediation record.

        Parameters
        ----------
        data
            Remediation record data
        include
            Specifies which relations should be loaded on the returned Remediation model

        Returns
        -------
        prisma.models.Remediation
            The created Remediation record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Remediation record from just the required fields
        remediation = await Remediation.prisma().create(
            data={
                # data to create a Remediation record
                'title': 'gdcgcgagj',
                'type': 'bhceabbgja',
                'snippet': 'ehabfhegh',
                'description': 'bcajcajjbc',
                'vulnerabilityId': 'bfdgheeegf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.RemediationCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Remediation records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Remediation record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Remediation.prisma().create_many(
            data=[
                {
                    # data to create a Remediation record
                    'title': 'ececbijji',
                    'type': 'cbcfgdcdhf',
                    'snippet': 'fdgjfbhia',
                    'description': 'jcehcdchh',
                    'vulnerabilityId': 'bgcbjdhjcc',
                },
                {
                    # data to create a Remediation record
                    'title': 'bieiidcabj',
                    'type': 'bjcbfcieaa',
                    'snippet': 'cbaaechiej',
                    'description': 'iejbeaaeg',
                    'vulnerabilityId': 'jcibfcbhf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.RemediationWhereUniqueInput,
        include: Optional[types.RemediationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Remediation record.

        Parameters
        ----------
        where
            Remediation filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Remediation model

        Returns
        -------
        prisma.models.Remediation
            The deleted Remediation record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        remediation = await Remediation.prisma().delete(
            where={
                'id': 'chdadcaga',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.RemediationWhereUniqueInput,
        include: Optional[types.RemediationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Remediation record.

        Parameters
        ----------
        where
            Remediation filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Remediation model

        Returns
        -------
        prisma.models.Remediation
            The found Remediation record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        remediation = await Remediation.prisma().find_unique(
            where={
                'id': 'jicieifbh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.RemediationWhereUniqueInput,
        include: Optional[types.RemediationInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Remediation record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Remediation filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Remediation model

        Returns
        -------
        prisma.models.Remediation
            The found Remediation record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        remediation = await Remediation.prisma().find_unique_or_raise(
            where={
                'id': 'fbahdheji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RemediationWhereInput] = None,
        cursor: Optional[types.RemediationWhereUniqueInput] = None,
        include: Optional[types.RemediationInclude] = None,
        order: Optional[Union[types.RemediationOrderByInput, List[types.RemediationOrderByInput]]] = None,
        distinct: Optional[List[types.RemediationScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Remediation records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Remediation records returned
        skip
            Ignore the first N results
        where
            Remediation filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Remediation model
        order
            Order the returned Remediation records by any field
        distinct
            Filter Remediation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Remediation]
            The list of all Remediation records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Remediation records
        remediations = await Remediation.prisma().find_many(take=10)

        # find the first 5 Remediation records ordered by the type field
        remediations = await Remediation.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RemediationWhereInput] = None,
        cursor: Optional[types.RemediationWhereUniqueInput] = None,
        include: Optional[types.RemediationInclude] = None,
        order: Optional[Union[types.RemediationOrderByInput, List[types.RemediationOrderByInput]]] = None,
        distinct: Optional[List[types.RemediationScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Remediation record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Remediation filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Remediation model
        order
            Order the returned Remediation records by any field
        distinct
            Filter Remediation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Remediation
            The first Remediation record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Remediation record ordered by the snippet field
        remediation = await Remediation.prisma().find_first(
            skip=1,
            order={
                'snippet': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RemediationWhereInput] = None,
        cursor: Optional[types.RemediationWhereUniqueInput] = None,
        include: Optional[types.RemediationInclude] = None,
        order: Optional[Union[types.RemediationOrderByInput, List[types.RemediationOrderByInput]]] = None,
        distinct: Optional[List[types.RemediationScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Remediation record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Remediation filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Remediation model
        order
            Order the returned Remediation records by any field
        distinct
            Filter Remediation records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Remediation
            The first Remediation record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Remediation record ordered by the description field
        remediation = await Remediation.prisma().find_first_or_raise(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.RemediationUpdateInput,
        where: types.RemediationWhereUniqueInput,
        include: Optional[types.RemediationInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Remediation record.

        Parameters
        ----------
        data
            Remediation record data specifying what to update
        where
            Remediation filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Remediation model

        Returns
        -------
        prisma.models.Remediation
            The updated Remediation record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        remediation = await Remediation.prisma().update(
            where={
                'id': 'cbbheiicgh',
            },
            data={
                # data to update the Remediation record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.RemediationWhereUniqueInput,
        data: types.RemediationUpsertInput,
        include: Optional[types.RemediationInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Remediation filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Remediation model

        Returns
        -------
        prisma.models.Remediation
            The created or updated Remediation record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        remediation = await Remediation.prisma().upsert(
            where={
                'id': 'beabjeejdg',
            },
            data={
                'create': {
                    'id': 'beabjeejdg',
                    'title': 'bieiidcabj',
                    'type': 'bjcbfcieaa',
                    'snippet': 'cbaaechiej',
                    'description': 'iejbeaaeg',
                    'vulnerabilityId': 'jcibfcbhf',
                },
                'update': {
                    'title': 'bieiidcabj',
                    'type': 'bjcbfcieaa',
                    'snippet': 'cbaaechiej',
                    'description': 'iejbeaaeg',
                    'vulnerabilityId': 'jcibfcbhf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.RemediationUpdateManyMutationInput,
        where: types.RemediationWhereInput,
    ) -> int:
        """Update multiple Remediation records

        Parameters
        ----------
        data
            Remediation data to update the selected Remediation records to
        where
            Filter to select the Remediation records to update

        Returns
        -------
        int
            The total number of Remediation records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Remediation records
        total = await Remediation.prisma().update_many(
            data={
                'prBody': 'bcjhgahffd'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RemediationWhereInput] = None,
        cursor: Optional[types.RemediationWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Remediation records present in the database

        Parameters
        ----------
        select
            Select the Remediation fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Remediation filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RemediationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Remediation.prisma().count()

        # results: prisma.types.RemediationCountAggregateOutput
        results = await Remediation.prisma().count(
            select={
                '_all': True,
                'vulnerabilityId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.RemediationCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RemediationWhereInput] = None,
        cursor: Optional[types.RemediationWhereUniqueInput] = None,
    ) -> types.RemediationCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.RemediationCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RemediationWhereInput] = None,
        cursor: Optional[types.RemediationWhereUniqueInput] = None,
    ) -> Union[int, types.RemediationCountAggregateOutput]:
        """Count the number of Remediation records present in the database

        Parameters
        ----------
        select
            Select the Remediation fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Remediation filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RemediationCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Remediation.prisma().count()

        # results: prisma.types.RemediationCountAggregateOutput
        results = await Remediation.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RemediationCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.RemediationWhereInput] = None
    ) -> int:
        """Delete multiple Remediation records.

        Parameters
        ----------
        where
            Optional Remediation filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Remediation records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Remediation records
        total = await Remediation.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.RemediationScalarFieldKeys'],
        *,
        where: Optional['types.RemediationWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RemediationAvgAggregateInput'] = None,
        sum: Optional['types.RemediationSumAggregateInput'] = None,
        min: Optional['types.RemediationMinAggregateInput'] = None,
        max: Optional['types.RemediationMaxAggregateInput'] = None,
        having: Optional['types.RemediationScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RemediationCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RemediationScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RemediationScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RemediationGroupByOutput']:
        """Group Remediation records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Remediation fields to group records by
        where
            Remediation filter to select records
        take
            Limit the maximum number of Remediation records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RemediationGroupByOutput]
            A list of dictionaries representing the Remediation record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Remediation records by title values
        # and count how many records are in each group
        results = await Remediation.prisma().group_by(
            ['title'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ScanActivityActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ScanActivity]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ScanActivity.prisma().query_raw(
            'SELECT * FROM ScanActivity WHERE id = ?',
            'fbjeiiffa',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ScanActivity
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ScanActivity.prisma().query_first(
            'SELECT * FROM ScanActivity WHERE status = ?',
            'jhgidcgbf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ScanActivityCreateInput,
        include: Optional[types.ScanActivityInclude] = None
    ) -> _PrismaModelT:
        """Create a new ScanActivity record.

        Parameters
        ----------
        data
            ScanActivity record data
        include
            Specifies which relations should be loaded on the returned ScanActivity model

        Returns
        -------
        prisma.models.ScanActivity
            The created ScanActivity record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ScanActivity record from just the required fields
        scanactivity = await ScanActivity.prisma().create(
            data={
                # data to create a ScanActivity record
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ScanActivityCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ScanActivity records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ScanActivity record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ScanActivity.prisma().create_many(
            data=[
                {
                    # data to create a ScanActivity record
                },
                {
                    # data to create a ScanActivity record
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ScanActivityWhereUniqueInput,
        include: Optional[types.ScanActivityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ScanActivity record.

        Parameters
        ----------
        where
            ScanActivity filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ScanActivity model

        Returns
        -------
        prisma.models.ScanActivity
            The deleted ScanActivity record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scanactivity = await ScanActivity.prisma().delete(
            where={
                'id': 'bgjgecfejc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ScanActivityWhereUniqueInput,
        include: Optional[types.ScanActivityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ScanActivity record.

        Parameters
        ----------
        where
            ScanActivity filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ScanActivity model

        Returns
        -------
        prisma.models.ScanActivity
            The found ScanActivity record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scanactivity = await ScanActivity.prisma().find_unique(
            where={
                'id': 'bgjcgchib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ScanActivityWhereUniqueInput,
        include: Optional[types.ScanActivityInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ScanActivity record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ScanActivity filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ScanActivity model

        Returns
        -------
        prisma.models.ScanActivity
            The found ScanActivity record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scanactivity = await ScanActivity.prisma().find_unique_or_raise(
            where={
                'id': 'bacdaibgfa',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScanActivityWhereInput] = None,
        cursor: Optional[types.ScanActivityWhereUniqueInput] = None,
        include: Optional[types.ScanActivityInclude] = None,
        order: Optional[Union[types.ScanActivityOrderByInput, List[types.ScanActivityOrderByInput]]] = None,
        distinct: Optional[List[types.ScanActivityScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ScanActivity records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ScanActivity records returned
        skip
            Ignore the first N results
        where
            ScanActivity filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ScanActivity model
        order
            Order the returned ScanActivity records by any field
        distinct
            Filter ScanActivity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ScanActivity]
            The list of all ScanActivity records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ScanActivity records
        scanactivitys = await ScanActivity.prisma().find_many(take=10)

        # find the first 5 ScanActivity records ordered by the logs field
        scanactivitys = await ScanActivity.prisma().find_many(
            take=5,
            order={
                'logs': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScanActivityWhereInput] = None,
        cursor: Optional[types.ScanActivityWhereUniqueInput] = None,
        include: Optional[types.ScanActivityInclude] = None,
        order: Optional[Union[types.ScanActivityOrderByInput, List[types.ScanActivityOrderByInput]]] = None,
        distinct: Optional[List[types.ScanActivityScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ScanActivity record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ScanActivity filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ScanActivity model
        order
            Order the returned ScanActivity records by any field
        distinct
            Filter ScanActivity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ScanActivity
            The first ScanActivity record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ScanActivity record ordered by the startedAt field
        scanactivity = await ScanActivity.prisma().find_first(
            skip=1,
            order={
                'startedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScanActivityWhereInput] = None,
        cursor: Optional[types.ScanActivityWhereUniqueInput] = None,
        include: Optional[types.ScanActivityInclude] = None,
        order: Optional[Union[types.ScanActivityOrderByInput, List[types.ScanActivityOrderByInput]]] = None,
        distinct: Optional[List[types.ScanActivityScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ScanActivity record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ScanActivity filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ScanActivity model
        order
            Order the returned ScanActivity records by any field
        distinct
            Filter ScanActivity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ScanActivity
            The first ScanActivity record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ScanActivity record ordered by the endedAt field
        scanactivity = await ScanActivity.prisma().find_first_or_raise(
            skip=1,
            order={
                'endedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ScanActivityUpdateInput,
        where: types.ScanActivityWhereUniqueInput,
        include: Optional[types.ScanActivityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ScanActivity record.

        Parameters
        ----------
        data
            ScanActivity record data specifying what to update
        where
            ScanActivity filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ScanActivity model

        Returns
        -------
        prisma.models.ScanActivity
            The updated ScanActivity record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        scanactivity = await ScanActivity.prisma().update(
            where={
                'id': 'dchgibach',
            },
            data={
                # data to update the ScanActivity record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ScanActivityWhereUniqueInput,
        data: types.ScanActivityUpsertInput,
        include: Optional[types.ScanActivityInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ScanActivity filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ScanActivity model

        Returns
        -------
        prisma.models.ScanActivity
            The created or updated ScanActivity record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scanactivity = await ScanActivity.prisma().upsert(
            where={
                'id': 'fchheijjc',
            },
            data={
                'create': {
                    'id': 'fchheijjc',
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ScanActivityUpdateManyMutationInput,
        where: types.ScanActivityWhereInput,
    ) -> int:
        """Update multiple ScanActivity records

        Parameters
        ----------
        data
            ScanActivity data to update the selected ScanActivity records to
        where
            Filter to select the ScanActivity records to update

        Returns
        -------
        int
            The total number of ScanActivity records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ScanActivity records
        total = await ScanActivity.prisma().update_many(
            data={
                'id': 'cacjdfhejh'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScanActivityWhereInput] = None,
        cursor: Optional[types.ScanActivityWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ScanActivity records present in the database

        Parameters
        ----------
        select
            Select the ScanActivity fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ScanActivity filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScanActivityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ScanActivity.prisma().count()

        # results: prisma.types.ScanActivityCountAggregateOutput
        results = await ScanActivity.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ScanActivityCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScanActivityWhereInput] = None,
        cursor: Optional[types.ScanActivityWhereUniqueInput] = None,
    ) -> types.ScanActivityCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ScanActivityCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScanActivityWhereInput] = None,
        cursor: Optional[types.ScanActivityWhereUniqueInput] = None,
    ) -> Union[int, types.ScanActivityCountAggregateOutput]:
        """Count the number of ScanActivity records present in the database

        Parameters
        ----------
        select
            Select the ScanActivity fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ScanActivity filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScanActivityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ScanActivity.prisma().count()

        # results: prisma.types.ScanActivityCountAggregateOutput
        results = await ScanActivity.prisma().count(
            select={
                '_all': True,
                'logs': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ScanActivityCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ScanActivityWhereInput] = None
    ) -> int:
        """Delete multiple ScanActivity records.

        Parameters
        ----------
        where
            Optional ScanActivity filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ScanActivity records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ScanActivity records
        total = await ScanActivity.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ScanActivityScalarFieldKeys'],
        *,
        where: Optional['types.ScanActivityWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ScanActivityAvgAggregateInput'] = None,
        sum: Optional['types.ScanActivitySumAggregateInput'] = None,
        min: Optional['types.ScanActivityMinAggregateInput'] = None,
        max: Optional['types.ScanActivityMaxAggregateInput'] = None,
        having: Optional['types.ScanActivityScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ScanActivityCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ScanActivityScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ScanActivityScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ScanActivityGroupByOutput']:
        """Group ScanActivity records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ScanActivity fields to group records by
        where
            ScanActivity filter to select records
        take
            Limit the maximum number of ScanActivity records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ScanActivityGroupByOutput]
            A list of dictionaries representing the ScanActivity record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ScanActivity records by startedAt values
        # and count how many records are in each group
        results = await ScanActivity.prisma().group_by(
            ['startedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IdorTestEvidenceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IdorTestEvidence]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await IdorTestEvidence.prisma().query_raw(
            'SELECT * FROM IdorTestEvidence WHERE id = ?',
            'bdbifjhbbi',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IdorTestEvidence
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await IdorTestEvidence.prisma().query_first(
            'SELECT * FROM IdorTestEvidence WHERE timestamp = ?',
            datetime.datetime.utcnow(),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.IdorTestEvidenceCreateInput,
        include: Optional[types.IdorTestEvidenceInclude] = None
    ) -> _PrismaModelT:
        """Create a new IdorTestEvidence record.

        Parameters
        ----------
        data
            IdorTestEvidence record data
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model

        Returns
        -------
        prisma.models.IdorTestEvidence
            The created IdorTestEvidence record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IdorTestEvidence record from just the required fields
        idortestevidence = await IdorTestEvidence.prisma().create(
            data={
                # data to create a IdorTestEvidence record
                'endpoint': 'cbccbbcdfb',
                'requestUserA': 'bacejedaca',
                'requestUserB': 'bhbhdahfaj',
                'responseA': 'bfjibceaec',
                'responseB': 'ibhgcdbgd',
                'status': 'badaffhddg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.IdorTestEvidenceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IdorTestEvidence records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IdorTestEvidence record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await IdorTestEvidence.prisma().create_many(
            data=[
                {
                    # data to create a IdorTestEvidence record
                    'endpoint': 'bbdbfcfihd',
                    'requestUserA': 'cbagggbji',
                    'requestUserB': 'bchgafhjed',
                    'responseA': 'heffgjdei',
                    'responseB': 'dahihgbeb',
                    'status': 'bgheaejbcc',
                },
                {
                    # data to create a IdorTestEvidence record
                    'endpoint': 'bfcgifeged',
                    'requestUserA': 'jfiahhbae',
                    'requestUserB': 'bfbdafajcb',
                    'responseA': 'caeghehde',
                    'responseB': 'caghgfbggd',
                    'status': 'bbidjbbjaa',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.IdorTestEvidenceWhereUniqueInput,
        include: Optional[types.IdorTestEvidenceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IdorTestEvidence record.

        Parameters
        ----------
        where
            IdorTestEvidence filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model

        Returns
        -------
        prisma.models.IdorTestEvidence
            The deleted IdorTestEvidence record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        idortestevidence = await IdorTestEvidence.prisma().delete(
            where={
                'id': 'bfijhaejdd',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.IdorTestEvidenceWhereUniqueInput,
        include: Optional[types.IdorTestEvidenceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IdorTestEvidence record.

        Parameters
        ----------
        where
            IdorTestEvidence filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model

        Returns
        -------
        prisma.models.IdorTestEvidence
            The found IdorTestEvidence record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        idortestevidence = await IdorTestEvidence.prisma().find_unique(
            where={
                'id': 'bcedehfiji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.IdorTestEvidenceWhereUniqueInput,
        include: Optional[types.IdorTestEvidenceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IdorTestEvidence record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IdorTestEvidence filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model

        Returns
        -------
        prisma.models.IdorTestEvidence
            The found IdorTestEvidence record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        idortestevidence = await IdorTestEvidence.prisma().find_unique_or_raise(
            where={
                'id': 'bdgjicijhb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdorTestEvidenceWhereInput] = None,
        cursor: Optional[types.IdorTestEvidenceWhereUniqueInput] = None,
        include: Optional[types.IdorTestEvidenceInclude] = None,
        order: Optional[Union[types.IdorTestEvidenceOrderByInput, List[types.IdorTestEvidenceOrderByInput]]] = None,
        distinct: Optional[List[types.IdorTestEvidenceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IdorTestEvidence records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IdorTestEvidence records returned
        skip
            Ignore the first N results
        where
            IdorTestEvidence filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model
        order
            Order the returned IdorTestEvidence records by any field
        distinct
            Filter IdorTestEvidence records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IdorTestEvidence]
            The list of all IdorTestEvidence records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IdorTestEvidence records
        idortestevidences = await IdorTestEvidence.prisma().find_many(take=10)

        # find the first 5 IdorTestEvidence records ordered by the endpoint field
        idortestevidences = await IdorTestEvidence.prisma().find_many(
            take=5,
            order={
                'endpoint': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IdorTestEvidenceWhereInput] = None,
        cursor: Optional[types.IdorTestEvidenceWhereUniqueInput] = None,
        include: Optional[types.IdorTestEvidenceInclude] = None,
        order: Optional[Union[types.IdorTestEvidenceOrderByInput, List[types.IdorTestEvidenceOrderByInput]]] = None,
        distinct: Optional[List[types.IdorTestEvidenceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IdorTestEvidence record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IdorTestEvidence filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model
        order
            Order the returned IdorTestEvidence records by any field
        distinct
            Filter IdorTestEvidence records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IdorTestEvidence
            The first IdorTestEvidence record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IdorTestEvidence record ordered by the requestUserA field
        idortestevidence = await IdorTestEvidence.prisma().find_first(
            skip=1,
            order={
                'requestUserA': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IdorTestEvidenceWhereInput] = None,
        cursor: Optional[types.IdorTestEvidenceWhereUniqueInput] = None,
        include: Optional[types.IdorTestEvidenceInclude] = None,
        order: Optional[Union[types.IdorTestEvidenceOrderByInput, List[types.IdorTestEvidenceOrderByInput]]] = None,
        distinct: Optional[List[types.IdorTestEvidenceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IdorTestEvidence record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IdorTestEvidence filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model
        order
            Order the returned IdorTestEvidence records by any field
        distinct
            Filter IdorTestEvidence records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IdorTestEvidence
            The first IdorTestEvidence record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IdorTestEvidence record ordered by the requestUserB field
        idortestevidence = await IdorTestEvidence.prisma().find_first_or_raise(
            skip=1,
            order={
                'requestUserB': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.IdorTestEvidenceUpdateInput,
        where: types.IdorTestEvidenceWhereUniqueInput,
        include: Optional[types.IdorTestEvidenceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IdorTestEvidence record.

        Parameters
        ----------
        data
            IdorTestEvidence record data specifying what to update
        where
            IdorTestEvidence filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model

        Returns
        -------
        prisma.models.IdorTestEvidence
            The updated IdorTestEvidence record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        idortestevidence = await IdorTestEvidence.prisma().update(
            where={
                'id': 'bghifjdeia',
            },
            data={
                # data to update the IdorTestEvidence record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.IdorTestEvidenceWhereUniqueInput,
        data: types.IdorTestEvidenceUpsertInput,
        include: Optional[types.IdorTestEvidenceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IdorTestEvidence filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IdorTestEvidence model

        Returns
        -------
        prisma.models.IdorTestEvidence
            The created or updated IdorTestEvidence record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        idortestevidence = await IdorTestEvidence.prisma().upsert(
            where={
                'id': 'eadfcbbcb',
            },
            data={
                'create': {
                    'id': 'eadfcbbcb',
                    'endpoint': 'bfcgifeged',
                    'requestUserA': 'jfiahhbae',
                    'requestUserB': 'bfbdafajcb',
                    'responseA': 'caeghehde',
                    'responseB': 'caghgfbggd',
                    'status': 'bbidjbbjaa',
                },
                'update': {
                    'endpoint': 'bfcgifeged',
                    'requestUserA': 'jfiahhbae',
                    'requestUserB': 'bfbdafajcb',
                    'responseA': 'caeghehde',
                    'responseB': 'caghgfbggd',
                    'status': 'bbidjbbjaa',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.IdorTestEvidenceUpdateManyMutationInput,
        where: types.IdorTestEvidenceWhereInput,
    ) -> int:
        """Update multiple IdorTestEvidence records

        Parameters
        ----------
        data
            IdorTestEvidence data to update the selected IdorTestEvidence records to
        where
            Filter to select the IdorTestEvidence records to update

        Returns
        -------
        int
            The total number of IdorTestEvidence records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IdorTestEvidence records
        total = await IdorTestEvidence.prisma().update_many(
            data={
                'responseA': 'geihgahba'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdorTestEvidenceWhereInput] = None,
        cursor: Optional[types.IdorTestEvidenceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IdorTestEvidence records present in the database

        Parameters
        ----------
        select
            Select the IdorTestEvidence fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IdorTestEvidence filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IdorTestEvidenceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await IdorTestEvidence.prisma().count()

        # results: prisma.types.IdorTestEvidenceCountAggregateOutput
        results = await IdorTestEvidence.prisma().count(
            select={
                '_all': True,
                'responseB': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.IdorTestEvidenceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdorTestEvidenceWhereInput] = None,
        cursor: Optional[types.IdorTestEvidenceWhereUniqueInput] = None,
    ) -> types.IdorTestEvidenceCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.IdorTestEvidenceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IdorTestEvidenceWhereInput] = None,
        cursor: Optional[types.IdorTestEvidenceWhereUniqueInput] = None,
    ) -> Union[int, types.IdorTestEvidenceCountAggregateOutput]:
        """Count the number of IdorTestEvidence records present in the database

        Parameters
        ----------
        select
            Select the IdorTestEvidence fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IdorTestEvidence filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IdorTestEvidenceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await IdorTestEvidence.prisma().count()

        # results: prisma.types.IdorTestEvidenceCountAggregateOutput
        results = await IdorTestEvidence.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IdorTestEvidenceCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.IdorTestEvidenceWhereInput] = None
    ) -> int:
        """Delete multiple IdorTestEvidence records.

        Parameters
        ----------
        where
            Optional IdorTestEvidence filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IdorTestEvidence records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IdorTestEvidence records
        total = await IdorTestEvidence.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.IdorTestEvidenceScalarFieldKeys'],
        *,
        where: Optional['types.IdorTestEvidenceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IdorTestEvidenceAvgAggregateInput'] = None,
        sum: Optional['types.IdorTestEvidenceSumAggregateInput'] = None,
        min: Optional['types.IdorTestEvidenceMinAggregateInput'] = None,
        max: Optional['types.IdorTestEvidenceMaxAggregateInput'] = None,
        having: Optional['types.IdorTestEvidenceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IdorTestEvidenceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IdorTestEvidenceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IdorTestEvidenceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IdorTestEvidenceGroupByOutput']:
        """Group IdorTestEvidence records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IdorTestEvidence fields to group records by
        where
            IdorTestEvidence filter to select records
        take
            Limit the maximum number of IdorTestEvidence records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IdorTestEvidenceGroupByOutput]
            A list of dictionaries representing the IdorTestEvidence record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IdorTestEvidence records by id values
        # and count how many records are in each group
        results = await IdorTestEvidence.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models